<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{path('home_index')}}">Mon agence</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" {% if current_meu is defined and current_menu == 'properties' %} active
                {% endif %} href="{{ path('property_index') }}">Acheter<span class="sr-only"></span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link"  href="{{ path('admin_index') }}">Admin<span class="sr-only"></span></a>
            </li>
         {#   <li class="nav-item active">
                {% if  app.user %}
                    <a class="nav-link"  href="{{ path('app_logout') }}">déconnexion<span class="sr-only"></span></a>
                    {% else %}
                    <a class="nav-link"  href="{{ path('security_registration') }}">connexion<span class="sr-only"></span></a>
                {% endif %}
            </li>#}
        </ul>
    </div>
</nav>
