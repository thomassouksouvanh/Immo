{% extends 'base.html.twig' %}

{% block title %}Optional{% endblock %}

{% block body %}
    <h1>Optional</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ optional.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ optional.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('optional_index') }}">back to list</a>

    <a href="{{ path('optional_edit', {'id': optional.id}) }}">edit</a>

    {{ include('optional/_delete_form.html.twig') }}
{% endblock %}
